[project]
name = "project-git-release"
version = "0.0.1"
dependencies = [
    "logging",
    "requests",
    "jsonpath-ng",
    "typer",
    "dotenv"
]

[tool.pytest.ini_options]
testpaths = ["test/pgr"]
pythonpath = ["src"]